export const metadata = {
  title: 'Contact',
  description: 'Have an idea? Let’s build it. Softoras contact for websites, SaaS, AI automation, and CRM systems.',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
